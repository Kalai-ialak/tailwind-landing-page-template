version: '3.8'

services:
  # Intha line kandippa venum
  prod-app:
    build:
      context: .
    container_name: tailwind-prod
    ports:
      - "8080:80"
    environment:
      - NODE_ENV=production
