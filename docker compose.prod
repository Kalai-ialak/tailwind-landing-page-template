
  # 2. Production Environment
  prod-app:
    build:
      context: .
      target: dockerfile.prod
    container_name: tailwind-prod
    ports:
      - "8080:80"
    environment:
      - NODE_ENV=production
